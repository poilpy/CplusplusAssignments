#ifndef FRACTION_H
#define FRACTION_H


class fraction
{
    public:
        fraction();
        virtual ~fraction();
        set(int num, int den);
        fraction addedTo();

    protected:

    private:

};

#endif // FRACTION_H
